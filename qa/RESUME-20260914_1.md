# 20260914_1 — PAUSED at the end of stage 3. How to resume.

Paused on user instruction, 2026-09-14, after the in-flight record update
finished. **Nothing is half-written**: the merged source, every artifact, the
ledger and the topics row all describe the same bytes, and `make check` was run
after the last edit to verify it.

## State, verified from `make check` (not from any agent's report)

**Repo-wide: 1 FAIL.** It is
`20260914_1: 詳細解説.json explains every keyed item (30 entries for 101 keys)`
— **expected and correct at this point.** The 30 entries present are the 聴解
ones `make mp3` writes; the 71 言語知識・読解 entries are stage 5's, which must not
run before QA passes. Every paper on disk passed through this same state.

**3 WARNs name this test**, all dispositioned, none repairable here:
1. `errand-rotation compares 1/44 = 2% of the draw` — a standing `pools.json`
   metadata debt. 33 of the 44 surfaces are `AUTHORED_THEME_CATS`, which carry no
   errand key BY DESIGN since 2026-09-07; the genuine half is `quick_response`
   at 1/11. `20260911_1` prints the same line at 2/44.
2. `問題8 form-family compares 1/5 = 20%` — the same hand-maintained-map debt;
   `20260911_1` prints it identically.
3. `聴解問題5 repeats a headline theme of 20260911_1 (スポーツ・余暇)` — the
   COMPOSED branch of rule 4, which is a WARN precisely because nothing is
   re-angleable: the clip is lifted from a real sitting and the only lever is
   seed-shopping, which the repo forbids (`exam-blueprint` rule 4).

The `pools_sha` line names this test only inside its "stamped on a REROLL" note;
sha `5632b5631716` matches `pools.json`.

## Provenance — the numbers any later stage must quote

- **Blueprint seed**: `48895101` (RNG, verified unused before the draw), plus a
  number of `--reroll-one` invocations this note deliberately DOES NOT restate.
  **Count them from `test_spec.json`'s `seed` field, which is authoritative and
  grows every time a finding is repaired by a redraw.** A restated count here has
  now gone stale twice on this one paper — written as "four" against 31
  (qa-report-20260914_1 F6), then corrected to "31" and staled the same day by
  F1/F2's own repairs (round 2, NEW-1). Full ledger string:
  `48895101+reroll-one(word_formation:2,57250112)+reroll-one(reading_topics:9,…)`
  — `test_spec.json` holds the authoritative string; read it, do not retype it.
- **聴解 composition seed: `91888352`** — the FINAL one. Seed `45256399` was the
  first composition and is HISTORY; it must not appear in any note as current.
  Source mix as shipped: official ×18, kanzenmoshi ×6, shinkanzen ×2, mimikara
  ×1, mondaireishuu ×1, soumatome ×1 = 29, from 8 sittings.
- The MP3 is uploaded and the manifest fingerprint matches the shipped bytes.

## What remains, in order

1. **Stage 4 — QA**, `exam-qa-review` in full, in a context that authored
   NOTHING. Fresh eyes is the one rule with no fallback.
2. **Stage 5 — model answer**, only after `QA: PASS`: author `詳細解説.json`
   (71 言語知識・読解 entries; the 30 聴解 ones already exist), then
   `詳細解説.vi.json` **in a separate context that has not seen the Japanese
   set**, then `make model-answer 20260914_1`, then `make check` again.
3. **Commit** `tests/20260914_1/` with the updated `logs/` and the two pipeline
   changes below, together.

## Hand the QA reviewer these, and say they are UNVERIFIED author claims

- `qa/stage3-report-20260914_1.md` — the build+gate report, its root-cause table
  (R1–R6, R4-bis), and §11 covering the re-composition and the three repairs.
- `qa/allocation-20260914_1.md` — the 13-surface allocation written before any
  prose existed.
- `qa/replan-20260914_1.md` + its AMENDMENT — why the listening half was re-drawn
  and why the clip pool did not grow.
- `qa/stage2-handoff-20260914_1.md` — the authors' own tables.

Open items QA must rule on, none of them defects in this paper:

- **問題12 was authored THREE times** (科学・技術 → 防災 → 教育). The ordering
  cause survives the fix: a 読解 headline theme is chosen at stage 1 against this
  paper's 聴解問題5, and stage 3 can re-draw the listening afterwards. Nothing
  between the two sees it. Fix = compose first, or re-check the headline set
  after every re-composition.
- **A repair applied to the merged `言語知識・読解.md` is provisional by
  construction** — the merge is one-directional from `_sections/`, so the next
  re-merge silently drops it, with no warning. This bit FOUR repairs in this run
  (three I caught via a rising FAIL count, one — R-d — found only because a
  truthful record re-derivation made a new FAIL appear). Candidate repair: a gate
  check that the merged file is reproducible from its fragments.
- **The `word_formation` sampler defect** (R-6 in the report): two distinct pool
  entries (`〜別`/`別〜`) whose PRINTED affix is identical, which 問題3 prints
  bare. Caught by an author measuring, not by a gate.
- **Kanji density 30.3 %** — inside the gate's 24.5–31 % window, 0.3 above the
  25–30 authoring band. Deliberately NOT traded back: novel vocabulary sits at
  15.21 % against an author target of ≤15.8 and a FAIL at 19.3, and spending that
  margin to fix a sub-band WARN is the wrong trade. QA may disagree.
- **問題11(1) / 問題11(2)** are adjacent in one 大問 and both open first-person
  naming the association the narrator belongs to. Judged ACCEPT twice (themes,
  narrators, closing shapes and claims all differ; no shared decisive detail);
  the repair, if QA wants one, is 問題11(1)'s opening angle, never a re-tag.
- **`〜ざるを得ない` appears as a printed distractor twice** (問題7-41 opt 2,
  問題9-50 opt 2) — exactly at the 2-item ceiling, so not a breach. It was
  wrongly dismissed once by a grep that could not match `休まざるを得ない`.

## Two pipeline changes are in the working tree, uncommitted

- `.agents/exam-blueprint/scripts/sample_items.py` (+147/−15) and its SKILL.md
  documentation: `--reroll-one` AND `--reroll` now redraw `AUTHORED_THEME_CATS`
  through `draw_authored_themes()`. Before this, either command silently reverted
  a `reading_topics`/`listening_scenarios` entry to the pre-2026-09-07 legacy
  `{theme, topic}` shape. Latent since 2026-09-07. Full-draw RNG stream measured
  byte-identical on 3 seeds; pool-category rerolls byte-identical on 5 categories.
- `.agents/choukai-audio/references/archive_bank_expansion.md` — DESIGN ONLY, no
  code. The queued work to bank the 21 older archive sittings.

## The queued follow-up (user directive, not started)

Expand the 聴解 bank beyond the 10 imported sittings. Route, cost and exclusions
are in `archive_bank_expansion.md`; the headline is ~15–25 sessions of
hand-verified work, spendable per ITEM so it can be aimed at the worst-worn 大問
(問題5) first. **`make choukai-wear` currently exits non-zero** (問題3 textbook
half projects 4.26 uses/clip against a 4.0 ceiling) — a pre-existing condition
whose prescribed fix is «grow the pool, never the slot count», i.e. this work.
